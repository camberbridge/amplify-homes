type PDFPolly @model @auth(rules: [{allow: public}]) {
  id: ID!
  userId: String
  bucketName: String
  documentStatus: String
  docObjectName: String
  pollyObjectName: String
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
}
 